<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="{{ LANG }}" lang="{{ LANG }}">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="{{ BASE_URI }}/css/screen-{{ VERSION }}.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css">
    <title>{{ title is defined ? title : APPLICATION_NAME }}</title>
    <script>
        const BASE_URL = '{{ BASE_URL }}',
              BASE_URI = '{{ BASE_URI }}';
    </script>
</head>

<body>
    <header class="navbar navbar-expand-sm shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="{{ BASE_URI }}">
                <img src="https://bloomington.in.gov/static/city-of-bloomington-logo.cfc9bd7.svg"
                     alt="City of Bloomington Logo" />
                {{ APPLICATION_NAME }}
            </a>
        </div>
    </header>
	<main>
        <div class="container">
            {% if errorMessages is defined %}
            {{ include('html/layouts/partials/errorMessages.twig') }}
            {% endif %}

            {% block content %}
            {% endblock %}
        </div>
	</main>
    <footer>
    </footer>
</body>
</html>
