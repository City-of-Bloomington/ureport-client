{#
 * @copyright 2024 City of Bloomington, Indiana
 * @license http://www.gnu.org/licenses/agpl.txt GNU/AGPL, see LICENSE
 * @param array groups
#}
{% extends "html/layouts/default.twig" %}

{% block content %}
<section id="chooseGroup" class="container">
    <header>
        <div class="alert alert-info">
            <h1 class="alert-heading fs-3">Non-Emergency Use Only</h1>
            <p>This system is not for reporting emergencies or imminent safety hazards.</p>
            <p>If this is an emergency, please dial 911.</p>
            <h1 class="alert-heading fs-3">Public Record</h1>
            <p>Information you provide through uReport is stored and retained by the City as a public record.</p>
        </div>
    </header>
</section>
<section class="container text-center">
    <div class="row row-cols-1 row-cols-md-2 g-3 justify-content-center">
    {% for code, group in groups %}
        <div class="col">
            <a href="{{ uri('home.service', {'group_code':code}) }}"class="p-3 btn btn-primary vstack align-items-center">
                <img src="{{ 'https://bloomington.in.gov/static/ureport-client' ~ uri('home.service', {'group_code':code}) ~ '.svg' }}" role="img" aria-hidden="true" class="mb-2" style="width:20%;">
            {{ group }}
            </a>
        </div>
    {% endfor %}
    </div>
</section>
{% endblock %}